# Toggle built-in sources without modifying Python code. Future RSS, Telegram,
# company career-page, and custom sources use the same registry contract.
sources:
  greenhouse: { enabled: true }
  linkedin: { enabled: false }
  telegram: { enabled: true }